# Release configuration fixtures

The production project identity is intentional. Publishable and signing-key values
in this folder are synthetic test bytes, not credentials or approved production
trust. They never become app configuration. The real reviewed manifest belongs at
`Config/Marketplace.production.json` only after provider and key-custody verification.
