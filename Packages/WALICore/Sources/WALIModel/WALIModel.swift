/// Metadata that confirms the WALI model module is available.
package enum WALIModelModule {
    /// The stable module name used by scaffold verification.
    package static let name = "WALIModel"
}
